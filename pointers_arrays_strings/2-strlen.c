#include "main.h"

/**
* _strlen - Programme pour return la longueur de la string.
* Return: 0
*/

int _strlen(char *s)
{
int longueur;
for (longueur = 0; longueur != *s; longueur++)
{
return (longueur);
}
return (0);
}
